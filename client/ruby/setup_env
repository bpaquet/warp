if [ -f "$0" ]; then
  DIRNAME=`dirname $0`
fi
if [ -f "$BASH_SOURCE" ]; then
  DIRNAME=`dirname $BASH_SOURCE`
fi
$DIRNAME/setup_ruby.sh
if [ -f .rbenv-gemsets ]; then
  $DIRNAME/setup_gemset.sh
fi
export PATH=$HOME/.rbenv/bin:$PATH
eval "$(rbenv init -)" || exit 1